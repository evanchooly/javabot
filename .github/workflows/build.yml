name: Build Project

on:
  push:
    branches-ignore:
      - 'dependabot/**'
  workflow_dispatch:

jobs:
  CodeAudits:
    uses: evanchooly/workflows/.github/workflows/code-audits.yml@master
    with:
      maven-flags: generate-resources
  Build:
    uses: evanchooly/workflows/.github/workflows/build.yml@master
    with:
      java: 17
      maven-flags: generate-resources
